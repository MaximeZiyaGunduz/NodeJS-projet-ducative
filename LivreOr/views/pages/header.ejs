<!DOCTYPE html>
<html>
    <head>
        <title>TEST</title>
        <link rel="stylesheet" href="/semantic/semantic.min.css">
        <style>
            .main.container{
                margin-top: 7em;
            }
        </style>
    </head>
        <body>

            <h1>sALUT</h1>

            <!--Renvoyer la valeur de test qui est dans server.js-->
            <p><%=test %></p>

            <div class="ui fixed inverted emnu">
                <a href="/" class="header item">Accueil</a>
            </div>
            <div class="ui main container">
                <!--pour afficher le msg d'erreur, Avant on vérifie l'existance de la variable locals-->
                <% if (locals.flash && locals.flash.error){ %>   
                <div class="ui negative message">
                    
                    <%= flash.error %> 
                </div>
                <%  } >